version: "3.2"

services:

  php:
    build: 
      context: "${PROJECT_DIR}/docker/php/"
      args:
       PHP_VERSION: ${DOCKER_PHP_VERSION}
    networks:
      - backend
    volumes:
      - ${WEB_ROOT_DIR}:/var/www/html/
      - ${WEB_LOGS_DIR}:/var/log/apache2/
    container_name: "${DOCKER_PHP_NAME}"

  apache:
    build:
      context: ${PROJECT_DIR}/docker/apache/
      args:
       APACHE_VERSION: ${DOCKER_APACHE_VERSION}
    depends_on:
      - php
      - mariadb
    networks:
      - backend
      - frontend
    ports:
      - "${DOCKER_APACHE_PORT}:80"
    volumes:
      - ${WEB_ROOT_DIR}:/var/www/html/
      - ${WEB_LOGS_DIR}:/var/log/apache2/
    container_name: "${DOCKER_APACHE_NAME}"

  mariadb:
    image: mariadb:${DOCKER_MARIADB_VERSION:-latest}
    restart: always
    ports:
      - "3306:3306"
    volumes:
      - ${PROJECT_DIR}/docker/mysql/data:/var/lib/mysql
    networks:
      - backend
    environment:
      MYSQL_ROOT_PASSWORD: "${DOCKER_DB_ROOT_PASSWORD}"
      MYSQL_DATABASE: "${DOCKER_DB_NAME}"
      MYSQL_USER: "${DOCKER_DB_USERNAME}"
      MYSQL_PASSWORD: "${DOCKER_DB_PASSWORD}"
    container_name: "${DOCKER_MARIADB_NAME}"

#  adminer:
#    image: adminer
#    ports:
#      - 8080:8080
#    networks:
#      - backend
#    restart: always
#    container_name: "${DOCKER_ADMINER_NAME}"

networks:
  backend:
  frontend:

